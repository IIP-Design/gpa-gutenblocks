{
  "name": "iip-gutenblocks",
  "version": "1.0.0",
  "private": true,
  "license": "GPL-2.0",
  "scripts": {
    "build:admin": "npm run build --prefix ./admin/blocks && npm run build --prefix ./admin/interface",
    "build:front": "npm run build --prefix ./public/blocks",
    "build": "npm run build:admin && npm run build:front",
    "setup:admin": "npm install --prefix ./admin/js",
    "setup:front": "npm install --prefix ./public/blocks",
    "setup": "npm run setup:admin && npm run setup:front"
  }
}
